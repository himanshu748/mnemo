fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
requests>=2.31
# Slack app
slack-bolt>=1.20
# Memory snapshot sync (MNEMO_HF_DATASET + HF_TOKEN) + the huggingface LLM
# provider's embeddings (needs Inference Providers routing support)
huggingface_hub>=0.27
# MCP server: shares the same memory with Claude Desktop/Code and other MCP clients
mcp>=1.9
starlette>=0.37
